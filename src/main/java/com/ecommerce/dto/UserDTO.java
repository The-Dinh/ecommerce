package com.ecommerce.dto;

import com.ecommerce.enumtype.Role;
import java.time.LocalDateTime;

public class UserDTO {
    // TODO: Add fields (id, fullName, email, password, phone, address, role, createdAt)
}
